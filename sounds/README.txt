Optional MP3s (e.g. Tuna / Voicemod exports). If a file is missing, the Narc falls back to spoken warnings only.

  observed.mp3    — plays when health drops to 75 (Observed)
  hostage.mp3     — plays at 30 (Hostage) when apps are hidden
  shame_zero.mp3  — plays at 0 when the Slack drought alert fires
  sip_success.mp3 — plays on full-compliance sip (smile S >= 0.28)
  sip_fail.mp3    — sip pose without sufficient smile
  rickroll.mp3    — optional local finale; if absent, macOS opens the Rickroll URL after wake
